#!/usr/bin/env bash

set -e
[ -n "$DOTFILE_DEBUG" ] && set -x

cd "$DOTFILES_DIR"

IFS=: paths=($PATH)

shopt -s nullglob

{ for path in "${paths[@]}"; do
    for command in "${path}/dotfile-"*; do
      command="${command##*dotfile-}"
      echo ${command}
    done
  done
} | sort | uniq
